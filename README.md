# Project-1-NumPy-Temperature-Analysis

Project 1: NumPy Temperature Analysis

Objective

The goal is to load simple temperature data, use NumPy for efficient data manipulation (specifically, conversion from Celsius to Fahrenheit), and perform basic descriptive statistics.
